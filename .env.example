# Zukerino Pastry Shop — front-end environment variables (Vite).
#
# These are OPTIONAL. The same values are already baked into
# src/supabaseConfig.js as defaults, so the site works without setting them.
# Set them in Vercel (Project → Settings → Environment Variables) if you prefer
# to keep the connection out of the code or to rotate keys later.
#
# Both values are PUBLIC by design (safe to expose in the browser):
#   - the anon/publishable key only allows READING the menu (row-level security)
#   - all menu edits go through a PIN-protected Supabase edge function
#
# Project: "Zukerino Pastry Shop" (Supabase)

VITE_SUPABASE_URL=https://gwynnkujfvvwhmccrlxj.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd3eW5ua3VqZnZ2d2htY2NybHhqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE0NDgxMTEsImV4cCI6MjA5NzAyNDExMX0.jPPM_G3QxFzygnezVUurlghwn7tz0cR_tlpPOMOYV-0
